#include "main.h"
#include <stdio.h>

/**
 * _abs - check the code
 * @y: An integer input
 * Description: This function returns absolute value of a number
 * Return: Absolut value of number r
 */

int _abs(int y)
{
if (y >= 0)
{
return (y);
}
else
{
return (y * -1);
}
}
